# ESP-Brookesia 组件接口速查表

> **快速查找组件接口的方法签名**
>
> **更新日期**：2025-01-04

---

## 📑 目录

- [systems/base](#systemsbase)
- [systems/speaker](#systemsspeaker)
- [gui](#gui)
- [ai_framework](#aiframework)
- [services](#services)

---

## systems/base

### Context

```cpp
// 初始化
bool begin(const Config& config);
bool del();

// 获取组件
Display& getDisplay();
Manager& getManager();
Event& getEvent();

// 事件注册
bool registerDateUpdateEventCallback(lv_event_cb_t callback, void* user_data = nullptr);
bool unregisterDateUpdateEventCallback(lv_event_cb_t callback, void* user_data = nullptr);
bool registerNavigateEventCallback(lv_event_cb_t callback, void* user_data = nullptr);
bool unregisterNavigateEventCallback(lv_event_cb_t callback, void* user_data = nullptr);
bool registerAppEventCallback(lv_event_cb_t callback, void* user_data = nullptr);
bool unregisterAppEventCallback(lv_event_cb_t callback, void* user_data = nullptr);

// 事件发送
bool sendDataUpdateEvent(void* param = nullptr);
bool sendNavigateEvent(Manager::NavigateType type);
bool sendAppEvent(const AppEventData* data);

// 线程安全
bool lockLv(int timeout = -1);
bool unlockLv();

// 应用管理
bool initAppFromRegistry(std::vector<RegistryAppInfo>& app_infos);
bool installAppFromRegistry(std::vector<RegistryAppInfo>& app_infos, std::vector<std::string>* ordered_app_names = nullptr);

// 获取信息
const Data& getData();
bool getDisplaySize(gui::StyleSize& size);
lv_display_t* getDisplayDevice();
lv_indev_t* getTouchDevice();
lv_obj_t* getEventObject();
lv_event_code_t getFreeEventCode();
lv_event_code_t getDataUpdateEventCode();
lv_event_code_t getNavigateEventCode();
lv_event_code_t getAppEventCode();
bool checkCoreInitialized();
bool checkAppID_Valid(int id);
```

### Manager

```cpp
// 初始化
bool begin();
bool del();

// 应用管理
int installApp(App* app);
int installApp(App& app);
bool uninstallApp(App* app);
bool uninstallApp(App& app);
bool uninstallApp(int id);

// 应用查询
App* getInstalledApp(int id);
App* getRunningAppByIdenx(uint8_t index);
App* getRunningAppById(int id);
App* getActiveApp();
const lv_draw_buf_t* getAppSnapshot(int id);

// 应用统计
uint8_t getRunningAppCount();
int getRunningAppIndexByApp(App* app);
int getRunningAppIndexById(int id);

// 批量操作
bool initAppFromRegistry(std::vector<RegistryAppInfo>& app_infos);
bool installAppFromRegistry(std::vector<RegistryAppInfo>& app_infos, std::vector<std::string>* ordered_app_names = nullptr);

// 配置
bool checkAppID_Valid(int id);
int getAppFreeId();
```

### App (虚函数接口)

```cpp
// 必须实现
virtual bool run() = 0;
virtual bool back() = 0;

// 可选重写
virtual bool init();
virtual bool deinit();
virtual bool pause();
virtual bool resume();
virtual bool close();
virtual bool cleanResource();

// 公共方法
bool checkInitialized();
int getId();
const char* getName();
const gui::StyleImage& getLauncherIcon();
const lv_area_t& getVisualArea();
const Config& getCoreInitData();
const Config& getCoreActiveData();
Context* getSystemContext();
bool notifyCoreClosed();
void setLauncherIconImage(const gui::StyleImage& icon_image);
bool startRecordResource();
bool endRecordResource();
bool cleanRecordResource();
lv_obj_t* getActiveScreen();
```

### Display

```cpp
// 调试
bool showContainerBorder();
bool hideContainerBorder();

// 屏幕管理
lv_obj_t* getMainScreen();
lv_obj_t* getSystemScreen();
lv_obj_t* getMainScreenObject();
lv_obj_t* getSystemScreenObject();
const gui::LvObject* getMainScreenPtr();
const gui::LvObject* getSystemScreenPtr();
const gui::LvObject* getMainScreenObjectPtr();
const gui::LvObject* getSystemScreenObjectPtr();

// 样式
lv_style_t* getCoreContainerStyle();

// 校准
bool calibrateCoreObjectSize(const gui::StyleSize& parent, gui::StyleSize& target);
bool calibrateCoreObjectSize(const gui::StyleSize& parent, gui::StyleSize& target_w, gui::StyleSize& target_h);
bool calibrateCoreFont(const gui::StyleSize* parent, gui::StyleFont& target);
bool calibrateCoreIconImage(const gui::StyleImage& target);

// 检查
bool checkCoreInitialized();
```

### Event

```cpp
// 重置
void reset();

// 事件管理
bool registerEvent(void* object, Handler handler, ID id, void* user_data = nullptr);
bool sendEvent(void* object, ID id, void* param = nullptr);
void unregisterEvent(多种重载);

// 获取空闲 ID
ID getFreeEventID();
```

---

## systems/speaker

### Speaker

```cpp
// 初始化
Speaker(lv_disp_t* display_device = nullptr);
bool begin();
bool del();

// 应用管理
int installApp(App* app);
int installApp(App& app);
bool uninstallApp(App* app);
bool uninstallApp(App& app);
bool uninstallApp(int id);

// 样式管理
bool addStylesheet(const Stylesheet& stylesheet);
bool activateStylesheet(const Stylesheet& stylesheet);

// 屏幕校准
bool calibrateScreenSize(gui::StyleSize& size);

// 组件访问
Display& getDisplay();
Manager& getManager();
```

### AI_Buddy

```cpp
// 单例模式
static std::shared_ptr<AI_Buddy> requestInstance();
static void releaseInstance();

// 初始化
bool begin(const Data& data);
bool resume();
bool pause();
bool del();

// 状态查询
bool isSpeaking();
bool isPause();
bool isWiFiValid();

// 音频事件
void sendAudioEvent(const AudioEvent& event);

// 表情访问
ai_framework::Expression expression;
```

### AppLauncher

```cpp
// 初始化
bool begin(lv_obj_t* parent);
bool del();

// 图标管理
bool addIcon(uint8_t page_index, const AppLauncherIconInfo_t& info);
bool removeIcon(int id);
bool changeIconTable(int id, uint8_t new_table_index);

// 滚动控制
bool scrollToPage(uint8_t index);
bool scrollToRightPage();
bool scrollToLeftPage();

// 查询
bool checkInitialized();
bool checkTableFull(uint8_t page_index);
bool checkVisible();
bool checkPointInsideMain(lv_point_t& point);
uint8_t getActiveScreenIndex();

// 校准
static bool calibrateData(const gui::StyleSize& screen_size, const base::Display& display, AppLauncherData& data);
```

### Keyboard

```cpp
// 初始化
bool begin(const gui::LvObject* parent);
bool del();

// 显示控制
bool setVisible(bool visible);

// 键盘模式
bool setMode(lv_keyboard_mode_t mode);
bool setTextEdit(lv_obj_t* text_edit);
bool setOkEnabled(bool enabled);

// 查询
bool isBegun();
bool isVisible();
bool getArea(lv_area_t& area);
bool getTextEdit(lv_obj_t*& text_edit);

// 信号
boost::signals2::signal<void(const std::string_view& text)> on_keyboard_value_changed_signal;
boost::signals2::signal<void(lv_event_t* e)> on_keyboard_draw_task_signal;

// 校准
static bool calibrateData(const gui::StyleSize& screen_size, const base::Display& display, KeyboardData& data);
```

### QuickSettings

```cpp
// 初始化
bool begin(const gui::LvObject& parent);
bool del();

// 显示控制
bool setVisible(bool visible);
bool moveY_To(int pos);
bool moveY_ToWithAnimation(int pos, bool is_visible_when_completed);
bool setAnimationCompletedMethod(gui::LvAnimation::CompletedMethod method);
bool setScrollable(bool enable);
bool scrollBack();

// 时间和 WiFi
bool setClockFormat(ClockFormat format);
bool setClockTime(int hour, int minute);
bool setWifiIconState(WifiState state);

// 电池和音量
bool setBatteryPercent(bool charge_flag, int percent);
bool setVolume(VolumeLevel level);
bool setVolume(int percent);
VolumeLevel getVolumeLevel();
int getVolumePercent();

// 亮度
bool setBrightness(BrightnessLevel level);
bool setBrightness(int percent);
BrightnessLevel getBrightnessLevel();
int getBrightnessPercent();

// 内存
bool setMemorySRAM(int percent);
bool setMemoryPSRAM(int percent);

// 查询
bool isBegun();
bool isVisible();
bool isAnimationRunning();
gui::LvObject* getWifiButton();
gui::LvObject* getVolumeButton();
gui::LvObject* getBrightnessButton();

// 事件连接
boost::signals2::connection connectEventSignal(EventSignalSlot slot);

// 校准
static bool calibrateData(const gui::StyleSize& screen_size, const base::Display& display, QuickSettingsData& data);
```

### Gesture

```cpp
// 初始化
bool begin(lv_obj_t* parent);
bool del();

// 触摸检测
bool readTouchPoint(int& x, int& y);

// 遮罩和指示器
bool setMaskObjectVisible(bool visible);
bool setIndicatorBarLength(GestureIndicatorBarType type, int length);
bool setIndicatorBarLengthByOffset(GestureIndicatorBarType type, int offset);
bool setIndicatorBarVisible(GestureIndicatorBarType type, bool visible);
bool controlIndicatorBarScaleBackAnim(GestureIndicatorBarType type, bool start);

// 查询
bool checkInitialized();
bool checkGestureStart();
bool checkMaskVisible();
bool checkIndicatorBarVisible(GestureIndicatorBarType type);
bool checkIndicatorBarScaleBackAnimRunning(GestureIndicatorBarType type);
int getIndicatorBarLength(GestureIndicatorBarType type);

// 事件
lv_obj_t* getEventObj();
lv_event_code_t getPressEventCode();
lv_event_code_t getPressingEventCode();
lv_event_code_t getReleaseEventCode();

// 校准
static bool calibrateData(const gui::StyleSize& screen_size, const base::Display& display, GestureData& data);
```

---

## gui

### StyleSize

```cpp
// 静态初始化
static constexpr StyleSize RECT(int w, int h);
static constexpr StyleSize RECT_PERCENT(int w_percent, int h_percent);
static constexpr StyleSize RECT_W_PERCENT(int w_percent, int h);
static constexpr StyleSize RECT_H_PERCENT(int w, int h_percent);
static constexpr StyleSize SQUARE(int size);
static constexpr StyleSize SQUARE_PERCENT(int percent);
static constexpr StyleSize CIRCLE(int size);
static constexpr StyleSize CIRCLE_PERCENT(int percent);

// 校准
bool calibrate(const StyleSize& parent);
bool calibrate(const StyleSize& parent, bool check_width, bool check_height);
bool calibrate(const StyleSize& parent, bool allow_zero);

// 成员
int width;
int height;
int radius;
int width_percent;
int height_percent;
struct {
    int enable_width_percent: 1;
    int enable_width_auto: 1;
    int enable_height_percent: 1;
    int enable_height_auto: 1;
    int enable_square: 1;
    int enable_circle: 1;
} flags;
```

### StyleFont

```cpp
// 静态初始化
static constexpr StyleFont SIZE(int size);
static constexpr StyleFont HEIGHT(int h);
static constexpr StyleFont HEIGHT_PERCENT(int percent);
static constexpr StyleFont CUSTOM_SIZE(int size, const void* font);

// 校准
bool calibrate(const StyleSize* parent, FindResourceBySizeMethod find_resource_by_size, FindResourceByHeightMethod find_resource_by_height, GetFontLineHeightMethod get_font_line_height);

// 成员
int size_px;
int height;
int height_percent;
const void* font_resource;
struct {
    int enable_height: 1;
    int enable_height_percent: 1;
} flags;
```

### StyleColor

```cpp
// 静态初始化
static constexpr StyleColor COLOR(uint32_t color24);
static constexpr StyleColor COLOR_WITH_OPACITY(uint32_t color24, uint8_t opa);

// 成员
uint32_t color: 24;
uint32_t opacity: 8;
```

### StyleImage

```cpp
// 静态初始化
static constexpr StyleImage IMAGE(const void* image);
static constexpr StyleImage IMAGE_RECOLOR(const void* image, uint32_t color);
static constexpr StyleImage IMAGE_RECOLOR_WHITE(const void* image);
static constexpr StyleImage IMAGE_RECOLOR_BLACK(const void* image);

// 校准
bool calibrate() const;

// 成员
const void* resource;
StyleColor recolor;
StyleColor container_color;
struct {
    int enable_recolor: 1;
    int enable_container_color: 1;
} flags;
```

### StyleAlign

```cpp
enum StyleAlignType {
    STYLE_ALIGN_TYPE_TOP_LEFT,
    STYLE_ALIGN_TYPE_TOP_MID,
    STYLE_ALIGN_TYPE_TOP_RIGHT,
    STYLE_ALIGN_TYPE_BOTTOM_LEFT,
    STYLE_ALIGN_TYPE_BOTTOM_MID,
    STYLE_ALIGN_TYPE_BOTTOM_RIGHT,
    STYLE_ALIGN_TYPE_LEFT_MID,
    STYLE_ALIGN_TYPE_RIGHT_MID,
    STYLE_ALIGN_TYPE_CENTER,
};

struct StyleAlign {
    StyleAlignType type;
    int offset_x;
    int offset_y;
};
```

### StyleGap

```cpp
// 静态初始化
static constexpr StyleGap AROUND(int top_v, int bottom_v, int left_v, int right_v);
static constexpr StyleGap ROW(int value);
static constexpr StyleGap COLUMN(int value);

// 成员
int top;
int bottom;
int left;
int right;
int row;
int column;
```

### StyleLayoutFlex

```cpp
enum FlowType {
    FLOW_ROW,
    FLOW_COLUMN,
    FLOW_ROW_WRAP,
    FLOW_ROW_REVERSE,
    FLOW_ROW_WRAP_REVERSE,
    FLOW_COLUMN_WRAP,
    FLOW_COLUMN_REVERSE,
    FLOW_COLUMN_WRAP_REVERSE,
};

enum AlignType {
    ALIGN_START,
    ALIGN_END,
    ALIGN_CENTER,
    ALIGN_SPACE_EVENLY,
    ALIGN_SPACE_AROUND,
    ALIGN_SPACE_BETWEEN,
};

struct StyleLayoutFlex {
    FlowType flow;
    AlignType main_place;
    AlignType cross_place;
    AlignType track_place;
};
```

### StyleAnimation

```cpp
enum AnimationPathType {
    ANIM_PATH_TYPE_LINEAR,
    ANIM_PATH_TYPE_EASE_IN,
    ANIM_PATH_TYPE_EASE_OUT,
    ANIM_PATH_TYPE_EASE_IN_OUT,
    ANIM_PATH_TYPE_OVERSHOOT,
    ANIM_PATH_TYPE_BOUNCE,
    ANIM_PATH_TYPE_STEP,
    ANIM_PATH_TYPE_MAX,
};

struct StyleAnimation {
    int start_value;
    int end_value;
    int duration_ms;
    int delay_ms;
    AnimationPathType path_type;
};
```

### StylesheetManager

```cpp
template <typename T>
class StylesheetManager {
public:
    // 样式表管理
    bool addStylesheet(const char* name, const StyleSize& screen_size, const T& stylesheet);
    bool activateStylesheet(const StyleSize& screen_size, const T& stylesheet);
    bool activateStylesheet(const char* name, const StyleSize& screen_size);

    // 样式表查询
    size_t getStylesheetCount();
    const T* getStylesheet();
    const T* getStylesheet(const char* name, const StyleSize& screen_size);
    const T* getStylesheet(const StyleSize& screen_size);

protected:
    // 纯虚函数
    virtual bool calibrateScreenSize(StyleSize& size) = 0;
};
```

### LvObject

```cpp
// 样式管理
bool setStyle(lv_style_t* style);
bool removeStyle(lv_style_t* style);
bool setStyleAttribute(多种重载);

// 位置管理
bool setX(int x);
bool setY(int y);
bool scrollY_To(int y, bool is_animated);
bool moveForeground();
bool moveBackground();

// 事件管理
bool addEventCallback(lv_event_cb_t cb, lv_event_code_t code, void* user_data = nullptr);
bool delEventCallback(多种重载);

// 查询
bool isValid();
bool hasState(lv_state_t state);
bool hasFlags(StyleFlag flags);
bool getX(int& x);
bool getY(int& y);
bool getArea(lv_area_t& area);
lv_obj_t* getNativeHandle();
```

### LvScreen

```cpp
LvScreen();
bool load();
```

### LvContainer

```cpp
LvContainer(const LvObject* parent);
```

### LvTimer

```cpp
// 类型定义
using TimerCallback = std::function<void(void*)>;

// 构造函数
LvTimer(TimerCallback callback, uint32_t period, void* user_data);

// 控制
bool pause();
bool resume();
bool restart();
bool reset();
bool setInterval(uint32_t interval_ms);

// 查询
bool isValid();
```

### LvAnimation

```cpp
// 类型定义
using VariableExecutionMethod = std::function<void(void* variable, int value)>;
using CompletedMethod = std::function<void(void* user_data)>;

// 配置
void setStyleAttribute(const StyleAnimation& attribute);
void setVariableExecutionMethod(void* variable, VariableExecutionMethod method);
void setCompletedMethod(CompletedMethod method);
void setUserData(void* user_data);

// 控制
bool start();
bool stop();
bool isRunning();
```

### LvLock

```cpp
// 类型定义
using LockCallback = std::function<bool(int timeout_ms)>;
using UnlockCallback = std::function<bool()>;

// 锁管理
bool lock(int timeout_ms = -1);
bool unlock();

// 单例
static LvLock& getInstance();
static void registerCallbacks(LockCallback lock_cb, UnlockCallback unlock_cb);
```

### LvLockGuard

```cpp
LvLockGuard();   // 构造时自动加锁
~LvLockGuard();  // 析构时自动解锁
```

### LvDisplay

```cpp
// 构造函数
LvDisplay(lv_disp_t* display, const LvDisplayData* data);

// 设备管理
bool setTouchDevice(lv_indev_t* touch);

// 调试
bool showContainerBorder();
bool hideContainerBorder();

// 字体
bool fontCalibrateMethod(StyleFont& target, const StyleSize* parent);
```

### LvHelper

```cpp
// 类型转换
lv_color_t toLvColor(uint32_t color);
lv_align_t toLvAlign(const StyleAlignType& type);
lv_text_align_t toLvTextAlign(const StyleAlignType& type);
lv_flex_flow_t toLvFlexFlow(const StyleLayoutFlex::FlowType& flow);
lv_flex_align_t toLvFlexAlign(const StyleLayoutFlex::AlignType& align);
lv_obj_flag_t toLvFlags(const StyleFlag& flags);

// 辅助函数
bool checkLvObjIsValid(lv_obj_t* obj);
bool checkLvObjOutOfParent(lv_obj_t* obj);
bool checkLvEventCodeValid(lv_event_code_t code);
bool getLvInternalFontBySize(uint8_t size_px, const lv_font_t** font);
lv_color_t getLvRandomColor(void);
lv_indev_t* getLvInputDev(const lv_display_t* display, lv_indev_type_t type);
lv_anim_path_cb_t getLvAnimPathCb(esp_brookesia::gui::StyleAnimation::AnimationPathType type);

// 创建对象
static LvObjSharedPtr createObj(lv_obj_t* parent);
static LvScreenUniquePtr createScreen();
static LvContainerUniquePtr createContainer(const LvObject* parent);
static LvTimerUniquePtr createTimer(TimerCallback func, uint32_t t, void* data);
static LvAnimationUniquePtr createAnimation();
```

### AnimPlayer

```cpp
// 初始化
bool begin(const AnimPlayerData& data);
bool del();

// 播放控制
bool sendEvent(const Event& event, bool clear_queue, EventFuture* future = nullptr);
bool notifyFlushFinished();

// 静态信号
static FlushReadySignal flush_ready_signal;
static AnimationStopSignal animation_stop_signal;
static AnimationEndSignal animation_end_signal;
```

---

## ai_framework

### Agent

```cpp
// 单例模式
static std::shared_ptr<Agent> requestInstance();
static void releaseInstance();

// 配置
bool configCozeAgentConfig(CozeChatAgentInfo& agent_info, std::vector<CozeChatRobotInfo>& robot_infos);
bool setCurrentRobotIndex(int index);
bool getCurrentRobotIndex(int& index);
bool getRobotInfo(int index, CozeChatRobotInfo& info);
bool getRobotInfo(std::vector<CozeChatRobotInfo>& infos);

// 生命周期
bool begin();
bool del();
bool pause();
bool resume();

// 聊天控制
bool sendChatEvent(const ChatEvent& event, bool clear_queue = true, int wait_finish_timeout_ms = 0);
bool hasChatState(ChatState state);
bool isChatState(ChatState state);

// 状态查询
bool isPaused();

// 工具
static std::string chatEventToString(const ChatEvent& event);
static std::string chatStateToString(const ChatState& state);

// 信号
ChatEventProcessSpecialSignal chat_event_process_special_signal;
ChatEventProcessStartSignal chat_event_process_start_signal;
ChatEventProcessEndSignal chat_event_process_end_signal;
```

### Expression

```cpp
// 初始化
bool begin(const ExpressionData& data, const EmojiMap* emoji_map, const SystemIconMap* system_icon_map);
bool del();
bool pause();
bool resume(bool update_emotion, bool update_icon);

// 表情控制
bool setEmoji(const std::string& emoji, const AnimOperationConfig& emotion_config, const AnimOperationConfig& icon_config);
bool setEmoji(const std::string& emoji);
bool insertEmojiTemporary(const std::string& emoji, uint32_t duration_ms = 1000);

// 系统图标
bool setSystemIcon(const std::string& icon, const AnimOperationConfig& config);
bool setSystemIcon(const std::string& icon);

// 常量
static constexpr int EMOTION_TYPE_NONE = gui::AnimPlayer::INDEX_NONE;
static constexpr int ICON_TYPE_NONE = gui::AnimPlayer::INDEX_NONE;
```

---

## services

### StorageNVS

```cpp
// 单例模式
static StorageNVS& requestInstance();

// 初始化
bool begin();

// 事件
bool sendEvent(const Event& event, EventFuture* future = nullptr);

// 参数操作
bool setLocalParam(const Key& key, const Value& value, const void* sender = nullptr, EventFuture* future = nullptr);
bool getLocalParam(const Key& key, Value& value);

// NVS 操作
bool eraseNVS(const void* sender = nullptr, EventFuture* future = nullptr);

// 事件订阅
boost::signals2::connection connectEventSignal(EventSignal::slot_type slot);
```

---

**快速查找完成！** 🎉

这个速查表包含了所有组件的公共接口方法签名，方便你在开发时快速查阅。
